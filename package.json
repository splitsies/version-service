{
  "name": "version-service",
  "version": "1.1.1",
  "description": "API for fetching version details",
  "main": "index.js",
  "scripts": {
    "build": "sam build",
    "clean": "rm -rf ./.aws-sam",
    "serve": "sam build && sam local start-api --host 0.0.0.0 --port 12908 --env-vars ./local-startup/env.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.14.14"
  }
}
